package graphs;

public class ArticulationPoint {
    // A vertex in an undirected connected graph is an articulation point (or cut vertex)
    // if removing it (and edges through it) disconnects the graph.

    // To find the Articulation Point, the modified Tarjan's Algorithm is used
    // Ancestor: a node A that was discoverd before current node in DFS, is the ancestor of current
    
    
    
}
